# Inventory Management System

## Features
- CRUD operations (Flask API)
- External API integration (OpenFoodFacts)
- CLI interface
- Unit testing with pytest

## Setup

```bash
git clone <your-repo-link>
cd inventory-system
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt